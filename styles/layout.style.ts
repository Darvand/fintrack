import { StyleSheet } from "react-native";

export const layout = StyleSheet.create({
  flexColumn: {
    flex: 1,
    flexDirection: "column",
  },
  flexRow: {
    flex: 1,
    flexDirection: "row",
  },
});
